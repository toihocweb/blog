# Installation

Getting started is incredibly easy.

## Prerequisites
Make sure you have Node.js installed on your machine. We recommend the latest LTS version.

## Setup
Run the following command in your terminal to bootstrap a new project:

```bash
npm create vite@latest my-app -- --template react-ts
cd my-app
npm install
npm run dev
```

That's it! Your application is now running locally.
